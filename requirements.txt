# ── Document extraction ───────────────────────────────────────────────────────
pymupdf>=1.24.0          # PDF text extraction (fitz)
python-docx>=1.1.0       # DOCX text extraction
Pillow>=10.0.0           # Image validation & preprocessing

# ── AI / Inference ────────────────────────────────────────────────────────────
ollama>=0.3.0            # Local Ollama client (qwen3.5:2b + nomic-embed-text)
openai>=1.0.0            # OpenAI-compatible client (hosted GPT-OSS & user_api paths)

# ── Vector stores ─────────────────────────────────────────────────────────────
chromadb>=0.5.0          # Local vector store (offline fallback, embedding cache)
moorcheh-sdk             # Hosted vector store (primary, cloud-synced)

# ── Knowledge graph ───────────────────────────────────────────────────────────
networkx>=3.3            # In-memory per-workspace knowledge graphs
numpy>=1.26.0            # Cosine similarity, vector arithmetic

# ── Metadata store ────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0        # SQLite ORM (file_records, workspaces, workspace_files)

# ── Agent framework ───────────────────────────────────────────────────────────
railtracks                # Tool-node agent framework (Bina Q&A agent)

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.111.0         # REST API sidecar on port 8765
uvicorn>=0.30.0          # ASGI server
python-multipart>=0.0.9  # FastAPI file upload support

# ── File watching ─────────────────────────────────────────────────────────────
watchdog>=4.0.0          # macOS FSEvents folder watcher

# ── CLI & utilities ───────────────────────────────────────────────────────────
click>=8.1.7             # CLI entrypoint (main.py)
rich>=13.7.0             # Terminal formatting (search REPL, progress)
